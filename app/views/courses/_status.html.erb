<div id="course_<%= course.id %>" class="course-status">
  <span class="status-badge status-<%= course.status %>"><%= course.status.humanize %></span>
  <% if course.failed? && course.validation_error.present? %>
    <p class="validation-error"><%= course.validation_error %></p>
  <% end %>
</div>
