<% flash.each do |type, message| %>
  <% style = type.to_s == "notice" ? "bg-green-50 text-green-800 border-green-200" : "bg-red-50 text-red-800 border-red-200" %>
  <div class="border rounded-lg px-4 py-3 mb-4 text-sm <%= style %>">
    <%= message %>
  </div>
<% end %>
